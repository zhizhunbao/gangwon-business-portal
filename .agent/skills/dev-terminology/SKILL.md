---
name: dev-terminology
description: 项目术语字典专家。确保江原道企业门户项目中所有命名遵循统一约定，保持前后端、测试、数据库的一致性。
---

# 项目术语字典工作流 (江原道企业门户专项)

确保项目中所有命名遵循统一约定，特别是处理韩语业务术语的英文映射。

## 触发场景

- 创建新模型/Schema（如：企业、申报、公告）
- 定义 API 端点
- 编写测试用例
- 创建前端组件/状态（特别是 i18n key 命名）

## 核心原则

### 1. 一词一义

每个术语只有一个明确定义。例如：企业统一用 `Enterprise`，避免混用 `Company`, `Business`, `Firm`。

### 2. 层级一致

- 数据库表名 → ORM 模型 → API Schema → 前端类型 → 测试

### 3. 语言约定

| 层级       | 语言      | 示例                                 |
| ---------- | --------- | ------------------------------------ |
| 代码标识符 | English   | `enterprise_id`, `notice_title`      |
| i18n Key   | camelCase | `common.save`, `auth.loginFailed`    |
| 业务注释   | 中文/韩文 | `// 企业申报服务 / 사업 신청 서비스` |

---

## 术语字典

> 📖 **完整术语表**: 见 `docs/glossary.md`

---

## 命名约定

### 后端 (Python)

- **类名**: PascalCase (`EnterpriseService`)
- **变量/函数**: snake_case (`get_enterprise_list`)

### 前端 (JavaScript/React)

- **文件名**: PascalCase (`NoticeList.jsx`)
- **变量/函数**: camelCase (`handleSubmit`)
- **i18n Key**: camelCase (`auth.login`)

### API 端点

- **路径**: kebab-case (`/api/v1/enterprise-applications`)
- **请求体**: snake_case (`{"enterprise_id": 123}`)

---

## 相关文件

- `docs/glossary.md` (术语对照表)
- `frontend/src/shared/i18n/locales/` (多语言翻译文件)

---

**记住**：一致的命名是可维护代码的基础。当不确定时，先查阅 `docs/glossary.md`！
